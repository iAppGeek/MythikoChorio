-- Mythiko Chorio — Development Seed Data
-- Run after schema.sql to populate a local/dev Supabase project.
-- Do NOT run against production.

-- Example: create a test admin user
-- (auth.users record must exist first — sign in once via the app, then run this)
--
-- INSERT INTO app_users (id, role, display_name, email)
-- VALUES ('auth-user-uuid-here', 'admin', 'Test Admin', 'admin@example.com');

-- Example: create a test guest profile
-- INSERT INTO student_profiles (is_guest, display_name, age, avatar_id)
-- VALUES (TRUE, 'Test Guest', 7, 'avatar_01');
